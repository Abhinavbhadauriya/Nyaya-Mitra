<h1>Welcome</h1>

<link href="https://cdn.jsdelivr.net/npm/@n8n/chat/dist/style.css" rel="stylesheet" />

<div id="n8n-chat"></div>

<script type="module">
  import { createChat } from 'https://cdn.jsdelivr.net/npm/@n8n/chat/dist/chat.bundle.es.js';

  // Create the chat widget
  createChat({
    webhookUrl: 'http://localhost:5678/webhook/00e5454e-71b0-4b80-9189-9a273b030828/chat',
    target: '#n8n-chat',
    mode: 'window',
    loadPreviousSession: true,
    enableStreaming: true,
    showWelcomeScreen: false,
    initialMessages: [
		'Hi there! 👋',
		'My name is NyayaMitra. What can I do for you today?😊'
	],
    i18n: {
      en: {
        
        title: 'NyayaMitra 🤖',
        subtitle: 'Ask me anything about court hearings or related data!',
        inputPlaceholder: 'Type your question...',
        getStarted: 'Start New Chat',
        
      },
    },
  });

  // Inject your theme dynamically AFTER chat loads
  const style = document.createElement('style');
  style.innerHTML = `
    :root {
      --chat--color-primary: #6c63ff;
      --chat--color-primary-shade-50: #5a54e6;
      --chat--color-primary-shade-100: #4a44d0;
      --chat--color-secondary: #00c9a7;
      --chat--color-secondary-shade-50: #00b395;
      --chat--color-secondary-shade-100: #009f85;
      --chat--color-light: #f4f6fa;
      --chat--color-dark: #0e0e2c;
      --chat--border-radius: 1rem;
      --chat--spacing: 1rem;
      --chat--window--width: 420px;
      --chat--window--height: 640px;
      --chat--font-family: 'Poppins', 'Segoe UI', Roboto, sans-serif;
      --chat--header--background: linear-gradient(135deg, var(--chat--color-primary), var(--chat--color-secondary));
      --chat--message--bot--background: #ffffffcc;
      --chat--message--user--background: var(--chat--color-primary);
      --chat--toggle--background: var(--chat--color-primary);
      --chat--footer--background: rgba(255, 255, 255, 0.65);
    }

    .chat-message {
      max-width: 65%;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      animation: fadeInUp 0.25s ease;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }

    ::-webkit-scrollbar {
      width: 8px;
    }
    ::-webkit-scrollbar-thumb {
      background-color: rgba(108, 99, 255, 0.4);
      border-radius: 4px;
    }
    ::-webkit-scrollbar-thumb:hover {
      background-color: rgba(108, 99, 255, 0.6);
    }
  `;
  document.head.appendChild(style);
</script>